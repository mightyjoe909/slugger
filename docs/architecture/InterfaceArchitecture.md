# Interface Architecture

> For the organization next MVP, IF-01, IF-02, IF-04, IF-06, IF-07, IF-10, IF-11, IF-12, IF-13, and IF-14 are active. IF-01 is required-string `execution_input_json` plus `concurrency_group`; IF-02 is canonical result delivery through the full-SHA pinned canonical receiver. [`docs/next-mvp.md`](../next-mvp.md) is the exact release boundary.

## Universal contract rules

Every interface has an owner and version; validates syntax, semantics, authorization, size, classification and cross-field invariants; carries stable operation/correlation identity; declares timeouts, retries and idempotency; emits typed safe errors; and supports conformance fixtures. Unknown major or required semantic fails closed. Sensitive values use opaque handles and never appear in logs/results. Retries use the same identity only for the same immutable input, exponential backoff with jitter and deadline, and no retry for policy/authority/version/ownership conflicts.

## Complete interface catalogue

| ID / interface | Responsibility and owner | Inputs → outputs | Validation / assumptions | Failure, retry, idempotency / versioning |
|---|---|---|---|---|
| IF-01 Canonical inbound | Receive routed work. Contract/control-plane owner; Slugger adapter owner. | Required `execution_input_json` and `concurrency_group` strings → acceptance/rejection | Pinned schema with format checking, authenticated caller, exact registered target, permitted task type/mode, draft-only; router enforces mutable activation before dispatch | Reject before mutation; at-least-once; `delivery_id` dedupes; changed payload/unknown major rejected. |
| IF-02 Canonical result | Send truthful execution outcome through the pinned organization receiver. | Run snapshot → schema-valid `execution-result/v2`, `source_issue` | Preserve delivery/correlation/target; sanitize; no invented status | Identical redelivery is safe; conflict fails closed; receiver acknowledgement is not execution success. |
| IF-03 Local CLI/API | Human/operator command and accessible views. Slugger. | Build/verify/resume/status/cancel arguments → structured + human result | Same policy as routed entry; explicit auth/target/scope; noninteractive mode | Stable exit categories; commands carry idempotency; interface semver/deprecation. |
| IF-04 Authority verifier | Verify externally owned current approval. Portfolio/control-plane asserts, Slugger consumes. | Authority/task/input/target refs → attributed decision + freshness/proof | Authenticity, scope, current revision, withdrawal/change; exact mechanism unknown | Unavailable/unknown blocks mutation; bounded transient retry; never cache beyond freshness. |
| IF-05 Capability catalog | Declare product support. Slugger product owner. | Class/mode/version → supported profile or denial | Promotion evidence and effective date; config cannot self-promote | Read/idempotent; release-versioned; absent/experimental denies supported status. |
| IF-06 Provider | Produce candidate under bounds. Provider implementation external; gateway Slugger. | Prompt/request digest, output contract, limits, cancellation → receipt/candidate/error/usage | Capability/version, quotas, response limits; all output untrusted | Retry only categorized pre-output failures within budget; operation ID prevents ambiguous duplicate; adapter semver/conformance. |
| IF-07 Workspace/storage | Allocate and access owned contained resources. Slugger logical contract; substrate external. | Run ownership/profile → handle; file operations → observations | Root containment, normalized paths, links/special files/quotas, ownership | Fail closed; cleanup idempotent by proof, never raw path; adapter version records control profile. |
| IF-08 Dependency source | Acquire allowed immutable dependencies. Source external; policy Slugger. | Approved coordinates/digests → verified bytes/metadata | Allow policy, lock/integrity/signature/license as required; egress controls | Bounded source retries; mismatch nonretryable; cache by verified digest, source/API version recorded. |
| IF-09 Sandbox runner | Execute generated behavior under controls. Slugger contract; substrate adapter. | Manifest/acquisition plan/commands/limits → exit, timeout, resource/output observations | Filesystem/process/network/credential/time/resource policy proven before start | Never auto-pass; kill on limit; command operation ID; ephemeral/recreate rather than unsafe resume; profile versioned. |
| IF-10 Evidence store | Persist immutable correlated evidence/audit. Slugger. | Records/classification/retention → digest/ref/read view | Schema, digests, access, atomicity, redaction derivation | Required write failure blocks progression; put by digest idempotent; schema evolution/migration explicit. |
| IF-11 Run repository | Persist aggregate/version/leases/outbox. Slugger. | Commands/expected version → committed snapshot/events | State transition invariants, atomic compare-and-set | Conflict reload/reconcile; no blind overwrite; owned schema versions and migrations. |
| IF-12 GitHub target | Preflight and hand off verified draft. Target/GitHub external; adapter Slugger. | Target/base/branch/marker/files/evidence → classification/commit/draft ref | Repo/base authority, permissions, exact inventory, marker, draft/open/head/base uniqueness | Ambiguity preserves state; stable publication ID; reconcile uncertain calls; API/marker semver. |
| IF-13 Secret broker | Provide least-privilege phase credentials. Credential authority external; use policy Slugger. | Workload/phase/purpose → opaque expiring handle | Actor/workload, scope, TTL, rotation, audit | No fallback plaintext; no generic retry after denial; retrieval idempotent only within lease; protocol versioned. |
| IF-14 Clock/digest/randomness | Supply testable foundational services. Slugger contract. | Canonical bytes/time/ID request → qualified value | Approved algorithms, monotonic duration, collision properties | Digest/algorithm change requires migration; run IDs random but delivery/publication IDs canonical. |
| IF-15 Observability sink | Export optional sanitized signals. Operator owns sink; Slugger owns emitter. | Structured event/metric/span → acknowledgement | Allowlisted fields, sampling, tenant/classification | Buffered/drop safely; never block mandatory evidence unless audit sink; schema versioned. |
| IF-16 Extension interface | Register approved bounded capability/adapter. Slugger product/security owner. | Signed manifest/contracts/conformance → registration decision | Compatibility, permissions, provenance, threats, promotion status | Reject/isolate on fault; no mandatory-gate bypass; extension API semver. |
| IF-17 Future knowledge input | Consume approved consulting/standards artifact. External owner supplies; Slugger adapts. | Versioned provenance/rights/classification/content → indexed immutable reference | Entire contract unknown; never execute embedded instructions | Disabled until validated; changed digest invalidates affected request/evidence; version pinned. |

## Public result shape (conceptual)

This shape is a local mapping checklist, **not** a schema. The organization-owned execution-result validator and vocabulary are authoritative. The adapter must cover verify/implement success, reuse, no-change, authorization/contract rejection, execution/validation/publication failure, and ambiguous/interrupted execution; unmappable semantics fail conformance.

All presentations expose: schema/contract version; delivery/task/correlation/run/attempt identities as permitted; scope and capability profile; lifecycle/terminal outcome; observed gate matrix; evidence digest/reference; publication occurrence/reference; safe categorized errors and retryability; residual risk/limitations; and the next accountable human action. Machine and human views must be semantically equivalent.

## Compatibility strategy

Semantic versions apply to Slugger-owned contracts. Additive optional fields may be backward compatible; changed meaning, required fields, identity, authorization, gate, or security behavior is breaking. Support windows and deprecation are release artifacts. In-flight runs remain pinned to their accepted contract/policy unless a security revocation explicitly blocks them. Cross-repository rollout order and downgrade behavior require owner agreement and fixtures.
